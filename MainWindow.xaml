<Window x:Class="RockPaperScissors_WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RockPaperScissors_WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Кнопки для выбора камня, ножниц, бумаги -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Center">
            <Button x:Name="btnRock" Content="Камень" Width="100" Margin="5"/>
            <Button x:Name="btnScissors" Content="Ножницы" Width="100" Margin="5"/>
            <Button x:Name="btnPaper" Content="Бумага" Width="100" Margin="5"/>
        </StackPanel>
        <!-- Окно для вывода результата -->
        <TextBox x:Name="txtResult" Grid.Row="2" Margin="10" IsReadOnly="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</Window>
